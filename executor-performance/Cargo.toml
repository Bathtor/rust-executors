[package]
name = "executor-performance"
version = "0.1.0"
authors = ["Lars Kroll <lkroll@kth.se>"]

[dependencies]
executors = { path = "../executors/" }
synchronoise = "0.4"
time = "0.1"

[dependencies.clap]
version = "2"
features = ["color"]